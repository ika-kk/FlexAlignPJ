<UserControl x:Class="FlexAlignPJ.Views.InputImageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlexAlignPJ.Views"
             xmlns:paz="clr-namespace:Scar.Common.WPF.Controls;assembly=Scar.Common.WPF.Controls.ZoomBorder"
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="450" d:DesignWidth="450">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="3"/>
        </Style>
        <Style TargetType="ToggleButton">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="3"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Button x:Name="ClearButton" Grid.Column="3" Content="clear" Click="ClearButton_Click"/>
        </Grid>

        <Border x:Name="CanvasParent" Grid.Row="1" Margin="3" Background="Gray">
            <Canvas x:Name="MyCanvas" ClipToBounds="True">
                <Image x:Name="MyImage" Source="{Binding Image, ElementName=Root}"
                       Width="{Binding ActualWidth, ElementName=CanvasParent}"
                       Height="{Binding ActualHeight, ElementName=CanvasParent}"/>
                <Image x:Name="MyMask" Opacity="0.5"
                       Width="{Binding ActualWidth, ElementName=CanvasParent}"
                       Height="{Binding ActualHeight, ElementName=CanvasParent}"
                       MouseLeftButtonDown="MyMask_MouseLeftButtonDown"
                       MouseMove="MyMask_MouseMove"
                       MouseLeftButtonUp="MyMask_MouseLeftButtonUp"/>
                <Rectangle x:Name="MyRect" Stroke="Magenta" StrokeThickness="3" Visibility="Hidden"
                       MouseLeftButtonUp="MyMask_MouseLeftButtonUp"/>
            </Canvas>
        </Border>

    </Grid>
</UserControl>
